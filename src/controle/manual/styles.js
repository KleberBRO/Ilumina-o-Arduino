import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerStyle:{
        width:"100%",
        height:"auto",
        alignItems:"center",
        paddingTop:150,
    },
    switch:{
        transform: [{scale: 2}]
    },
    inputLabel:{
        color: "#E0FFED",
        fontWeight:"bold",
        fontSize:25,
        paddingTop:20
    },
});

export default styles